<RelativeLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="fill_parent"
    android:layout_width="fill_parent"
    xmlns:couple="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootMain">
<android.support.v4.view.ViewPager
    android:id="@+id/pager"
    android:layout_width="match_parent" android:layout_height="match_parent"
    tools:context="com.franco.general.couple.main.MainActivity" />
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fabSend"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:onClick="sendAction"
        android:elevation="5dp"
        android:src="@drawable/ic_send_black_24dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        couple:backgroundTint="@color/couple_card_underline" />
    <android.support.design.widget.FloatingActionButton

        android:id="@+id/fabAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:onClick="diaryAction"
        android:elevation="5dp"
        android:src="@drawable/ic_add_black_24dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        couple:backgroundTint="@color/couple_red" />
    <android.support.design.widget.FloatingActionButton
        android:visibility="invisible"
        android:id="@+id/fabCamera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:onClick="cameraAction"
        android:elevation="5dp"
        android:src="@drawable/ic_camera_alt_black_24dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        couple:backgroundTint="@color/couple_orange" />

</RelativeLayout>
